# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libglpk-dev

language: R
sudo: false
warnings_are_errors: false

before_script:
  - Rscript -e 'library(describr); sessionInfo()'
